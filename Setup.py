from setuptools import setup, find_packages

setup(
    name="CTkKeyboard",
    version='0.1',
    author="Cadam",
    description='Virtual Keyboard for CustomTkinter',
    packages=find_packages(),
    install_requires=['customtkinter'], 
)